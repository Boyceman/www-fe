// import { getUserProfile } from '../api/profile'

export default {
  // fetchProfile ({ commit }) {
  //   return getUserProfile().then(({ extJson }) => {
  //     commit('userNickname', extJson)
  //     commit('userLogo', extJson)
  //   })
  // }
}
